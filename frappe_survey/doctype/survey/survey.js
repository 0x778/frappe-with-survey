// Copyright (c) 2023, Greycube and contributors
// For license information, please see license.txt

frappe.ui.form.on('Survey', {
	refresh: function(frm) {
		// let tITLE = "FDSGSDG";
		// frm.doc.title.addEventListener("change", (event) => {
		// 	textContent = `You like ${event.target.value}`;
		// 	console.log(textContent);
		// });

		// {
		// 	"title":titlevar,
		// }
	}
});
